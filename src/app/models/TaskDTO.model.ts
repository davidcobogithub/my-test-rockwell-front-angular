export interface TaskDTO {
  name: string;
  url: string;
  typetaskaction: string;
  cronexp: string;
  timerange: string;
  typetime: string;
  dateExecution:string;
  response:string;
  status:string;
}